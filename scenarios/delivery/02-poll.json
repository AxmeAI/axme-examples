{
  "scenario_id": "delivery.poll_agent.v1",
  "title": "Poll binding — agent periodically pulls pending intents",
  "description": "Agent does NOT hold a persistent connection. Instead it periodically calls GET /v1/agents/{address}/intents?since=cursor to check for new intents. Useful for batch processors, cron jobs, or environments where persistent connections are impractical.",

  "agents": [
    {
      "role": "batch_processor",
      "address": "batch-processor-agent",
      "display_name": "Batch Processor",
      "delivery_mode": "poll",
      "handler": {
        "type": "auto_complete",
        "delay_seconds": 0.5,
        "outcome": { "result": "batch_processed", "records_count": 42 }
      },
      "description": "step 1/1 — auto: polls for pending intents, processes batch"
    }
  ],

  "humans": [],

  "workflow_steps": [
    {
      "step_id": "process_batch",
      "tool_id": "tool.approval.check_window",
      "assigned_to": "batch_processor",
      "step_deadline_seconds": 120
    }
  ],

  "intent": {
    "type": "intent.example.poll.v1",
    "payload": {
      "batch_id":    "batch-2026-03-13-001",
      "record_type": "transactions",
      "date_range":  "2026-03-12"
    }
  },

  "durability": {
    "deadline_minutes": 10,
    "max_delivery_attempts": 5
  }
}
